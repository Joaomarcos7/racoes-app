# Fiado Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar página `/fiado` que centraliza todos os clientes com fiado em aberto e permite dar baixa sem sair da tela.

**Architecture:** Novo endpoint `GET /api/fiado` agrega clientes com pedidos em aberto. Hook `useFiado` wraps a query. Página hub renderiza tabela de clientes + `BaixaFiadoDialog` existente. Sidebar e PainelFiado ganham links de entrada para o hub.

**Tech Stack:** Next.js App Router, Prisma ORM, TanStack Query, Tailwind CSS, shadcn/ui, lucide-react.

**Spec:** `docs/superpowers/specs/2026-08-27-fiado-hub-design.md`

## Global Constraints

- Autenticação obrigatória em todos os endpoints: `const session = await auth(); if (!session) return NextResponse.json({ error: "Não autorizado" }, { status: 401 })`
- Importar `prisma` de `@/lib/prisma`, `auth` de `@/lib/auth`
- Queries apenas em pedidos com `ativo: true`
- Sem paginação na listagem de fiados — retorna tudo
- Não modificar `BaixaFiadoDialog` nem `useDarBaixaFiado`
- Commits com `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`

---

### Task 1: Endpoint `GET /api/fiado` + hook `useFiado`

**Files:**
- Create: `src/app/api/fiado/route.ts`
- Create: `src/hooks/use-fiado.ts`

**Interfaces:**
- Produces:
  ```ts
  // useFiado() return type
  interface ClienteFiadoHub {
    id: string
    nome: string
    cidade: string
    telefone: string | null
    totalFiado: number
    pedidosFiado: PedidoDTO[]
  }
  interface FiadoHubData {
    totalGeral: number
    clientes: ClienteFiadoHub[]
  }
  function useFiado(): UseQueryResult<FiadoHubData>
  ```

- [ ] **Step 1: Criar o endpoint**

Crie `src/app/api/fiado/route.ts` com o conteúdo abaixo. O endpoint:
- Autentica a sessão
- Busca clientes com ao menos um pedido `statusPagamento: "FIADO"`, `valorEmAbertoFiado > 0`, `ativo: true`
- Inclui esses pedidos (filtrados) com itens e produtos
- Calcula `totalFiado` por cliente e `totalGeral`
- Ordena por `totalFiado` decrescente

```ts
import { NextRequest, NextResponse } from "next/server"
import { prisma } from "@/lib/prisma"
import { auth } from "@/lib/auth"

export async function GET(_req: NextRequest) {
  const session = await auth()
  if (!session) return NextResponse.json({ error: "Não autorizado" }, { status: 401 })

  const clientes = await prisma.cliente.findMany({
    where: {
      ativo: true,
      pedidos: {
        some: { statusPagamento: "FIADO", valorEmAbertoFiado: { gt: 0 }, ativo: true },
      },
    },
    include: {
      pedidos: {
        where: { statusPagamento: "FIADO", valorEmAbertoFiado: { gt: 0 }, ativo: true },
        include: { itens: { include: { produto: true } } },
      },
    },
  })

  const result = clientes
    .map((c) => ({
      id: c.id,
      nome: c.nome,
      cidade: c.cidade,
      telefone: c.telefone,
      totalFiado: c.pedidos.reduce((acc, p) => acc + (p.valorEmAbertoFiado ?? 0), 0),
      pedidosFiado: c.pedidos,
    }))
    .sort((a, b) => b.totalFiado - a.totalFiado)

  const totalGeral = result.reduce((acc, c) => acc + c.totalFiado, 0)

  return NextResponse.json({ totalGeral, clientes: result })
}
```

- [ ] **Step 2: Criar o hook**

Crie `src/hooks/use-fiado.ts`:

```ts
import { useQuery } from "@tanstack/react-query"
import type { PedidoDTO } from "@/types/api"

export interface ClienteFiadoHub {
  id: string
  nome: string
  cidade: string
  telefone: string | null
  totalFiado: number
  pedidosFiado: PedidoDTO[]
}

export interface FiadoHubData {
  totalGeral: number
  clientes: ClienteFiadoHub[]
}

export function useFiado() {
  return useQuery<FiadoHubData>({
    queryKey: ["fiado"],
    queryFn: async () => {
      const res = await fetch("/api/fiado")
      if (!res.ok) throw new Error("Erro ao buscar fiados")
      return res.json()
    },
  })
}
```

- [ ] **Step 3: Testar o endpoint manualmente**

Inicie o servidor de desenvolvimento: `npm run dev`

Acesse `http://localhost:3000/api/fiado` no browser (com sessão ativa) ou via DevTools.

Verifique que a resposta tem a shape:
```json
{
  "totalGeral": 1234.56,
  "clientes": [
    {
      "id": "...",
      "nome": "...",
      "cidade": "...",
      "telefone": "...",
      "totalFiado": 500.00,
      "pedidosFiado": [...]
    }
  ]
}
```

Clientes com maior `totalFiado` aparecem primeiro.

- [ ] **Step 4: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/fiado/route.ts src/hooks/use-fiado.ts
git commit -m "$(cat <<'EOF'
feat: endpoint GET /api/fiado e hook useFiado

Agrega clientes com fiado em aberto, ordenados por total devido desc.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Página hub `/fiado`

**Files:**
- Create: `src/app/(system)/fiado/page.tsx`

**Interfaces:**
- Consumes:
  - `useFiado(): UseQueryResult<FiadoHubData>` — de `@/hooks/use-fiado`
  - `ClienteFiadoHub` — interface exportada de `@/hooks/use-fiado`
  - `useDarBaixaFiado(clienteId: string)` — de `@/hooks/use-clientes`
  - `BaixaFiadoDialog` — de `@/components/clientes/BaixaFiadoDialog`
  - `PageHeader` — de `@/components/layout/PageHeader`
  - `formatCurrency` — de `@/lib/utils`
  - `Badge`, `Button` — de `@/components/ui/badge` e `@/components/ui/button`
  - `useQueryClient` — de `@tanstack/react-query`
  - `CheckCircle2` — de `lucide-react`
  - `PedidoDTO` — de `@/types/api`

**Lógica da página:**
- `selected` state: `{ id: string; pedidosFiado: PedidoDTO[] } | null`
- `baixaMutation = useDarBaixaFiado(selected?.id ?? "")` — hook chamado incondicionalmente no topo
- Ao baixa: `onSuccess` invalida `["fiado"]` + fecha dialog
- `useDarBaixaFiado` já invalida `["clientes", clienteId]` e `["pedidos"]` internamente

- [ ] **Step 1: Criar a página**

Crie `src/app/(system)/fiado/page.tsx`:

```tsx
"use client"
import { useState } from "react"
import { useQueryClient } from "@tanstack/react-query"
import { PageHeader } from "@/components/layout/PageHeader"
import { BaixaFiadoDialog } from "@/components/clientes/BaixaFiadoDialog"
import { useFiado } from "@/hooks/use-fiado"
import { useDarBaixaFiado } from "@/hooks/use-clientes"
import { formatCurrency } from "@/lib/utils"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { CheckCircle2 } from "lucide-react"
import type { PedidoDTO } from "@/types/api"

interface ClienteSelected {
  id: string
  pedidosFiado: PedidoDTO[]
}

export default function FiadoPage() {
  const { data, isLoading } = useFiado()
  const qc = useQueryClient()
  const [selected, setSelected] = useState<ClienteSelected | null>(null)
  const baixaMutation = useDarBaixaFiado(selected?.id ?? "")

  if (isLoading) return <p className="text-sm text-gray-500">Carregando...</p>

  const clientes = data?.clientes ?? []
  const totalGeral = data?.totalGeral ?? 0

  return (
    <div>
      <PageHeader
        title="Fiado em Aberto"
        description={`${clientes.length} cliente${clientes.length !== 1 ? "s" : ""}`}
        action={
          <Badge className="bg-orange-100 text-orange-700 border-orange-200 text-sm px-3 py-1">
            {formatCurrency(totalGeral)}
          </Badge>
        }
      />

      {clientes.length === 0 ? (
        <div className="flex flex-col items-center justify-center py-20 text-gray-400 gap-3">
          <CheckCircle2 size={40} className="text-green-400" />
          <p className="text-sm">Nenhum fiado em aberto</p>
        </div>
      ) : (
        <div className="bg-white rounded-lg border overflow-hidden">
          <table className="w-full text-sm">
            <thead className="bg-gray-50 border-b">
              <tr>
                <th className="text-left px-4 py-3 font-medium text-gray-500">Cliente</th>
                <th className="text-left px-4 py-3 font-medium text-gray-500">Cidade</th>
                <th className="text-left px-4 py-3 font-medium text-gray-500">Telefone</th>
                <th className="text-center px-4 py-3 font-medium text-gray-500">Pedidos</th>
                <th className="text-right px-4 py-3 font-medium text-gray-500">Total devido</th>
                <th className="px-4 py-3" />
              </tr>
            </thead>
            <tbody className="divide-y">
              {clientes.map((c) => (
                <tr key={c.id} className="hover:bg-gray-50">
                  <td className="px-4 py-3 font-medium text-gray-900">{c.nome}</td>
                  <td className="px-4 py-3 text-gray-500">{c.cidade}</td>
                  <td className="px-4 py-3 text-gray-500">{c.telefone ?? "—"}</td>
                  <td className="px-4 py-3 text-center">
                    <Badge className="bg-orange-50 text-orange-600 border-orange-200">
                      {c.pedidosFiado.length}
                    </Badge>
                  </td>
                  <td className="px-4 py-3 text-right font-bold text-orange-600">
                    {formatCurrency(c.totalFiado)}
                  </td>
                  <td className="px-4 py-3 text-right">
                    <Button
                      size="sm"
                      className="bg-orange-600 hover:bg-orange-500"
                      onClick={() => setSelected({ id: c.id, pedidosFiado: c.pedidosFiado })}
                    >
                      Dar Baixa
                    </Button>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}

      <BaixaFiadoDialog
        open={!!selected}
        onOpenChange={(open) => { if (!open) setSelected(null) }}
        pedidosFiado={selected?.pedidosFiado ?? []}
        loading={baixaMutation.isPending}
        onSubmit={(data) =>
          baixaMutation.mutate(data, {
            onSuccess: () => {
              qc.invalidateQueries({ queryKey: ["fiado"] })
              setSelected(null)
            },
          })
        }
      />
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Testar na UI**

Com `npm run dev`:
- Acesse `http://localhost:3000/fiado`
- Verifique que a tabela carrega com os clientes com fiado
- Clique "Dar Baixa" em um cliente — dialog deve abrir com os pedidos desse cliente
- Registre uma baixa parcial — dialog fecha, linha atualiza (valor diminui ou some se quitado)
- Se todos os pedidos de um cliente foram quitados, o cliente não aparece mais na lista

- [ ] **Step 4: Commit**

```bash
git add "src/app/(system)/fiado/page.tsx"
git commit -m "$(cat <<'EOF'
feat: página hub /fiado com tabela e dar baixa inline

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Entry points — Sidebar e PainelFiado

**Files:**
- Modify: `src/components/layout/Sidebar.tsx`
- Modify: `src/components/dashboard/PainelFiado.tsx`

**Interfaces:**
- Consumes: `Link` de `next/link` (já importado em ambos os arquivos via padrão do projeto)

- [ ] **Step 1: Adicionar link "Fiado" na Sidebar**

Em `src/components/layout/Sidebar.tsx`, importe `Wallet` do lucide-react e adicione ao array `links` entre Pedidos e Clientes:

```ts
// Antes:
import {
  LayoutDashboard, Package, Users, ShoppingCart,
  Truck, ClipboardList, LogOut, Menu, X, TrendingDown
} from "lucide-react"

// Depois:
import {
  LayoutDashboard, Package, Users, ShoppingCart,
  Truck, ClipboardList, LogOut, Menu, X, TrendingDown, Wallet
} from "lucide-react"
```

```ts
// Antes:
const links = [
  { href: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  { href: "/pedidos", label: "Pedidos", icon: ShoppingCart },
  { href: "/clientes", label: "Clientes", icon: Users },
  { href: "/produtos", label: "Produtos", icon: Package },
  { href: "/veiculos", label: "Veículos", icon: Truck },
  { href: "/consolidacao", label: "Consolidação", icon: ClipboardList },
  { href: "/saidas", label: "Saídas", icon: TrendingDown },
]

// Depois:
const links = [
  { href: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  { href: "/pedidos", label: "Pedidos", icon: ShoppingCart },
  { href: "/fiado", label: "Fiado", icon: Wallet },
  { href: "/clientes", label: "Clientes", icon: Users },
  { href: "/produtos", label: "Produtos", icon: Package },
  { href: "/veiculos", label: "Veículos", icon: Truck },
  { href: "/consolidacao", label: "Consolidação", icon: ClipboardList },
  { href: "/saidas", label: "Saídas", icon: TrendingDown },
]
```

- [ ] **Step 2: Tornar o header do PainelFiado um link para `/fiado`**

Em `src/components/dashboard/PainelFiado.tsx`, adicione `import Link from "next/link"` no topo e envolva o título do card em `<Link>`:

```tsx
// Adicionar import no topo:
import Link from "next/link"

// Antes (linhas 18-23 aprox):
        <div className="flex items-center gap-2">
          <AlertTriangle size={16} className="text-orange-500 flex-shrink-0 mt-0.5" />
          <div>
            <h3 className="font-semibold text-sm text-gray-700">Fiado em Aberto</h3>
            <p className="text-xs text-gray-400">{clientes.length} clientes</p>
          </div>
        </div>

// Depois:
        <Link href="/fiado" className="flex items-center gap-2 hover:opacity-75 transition-opacity">
          <AlertTriangle size={16} className="text-orange-500 flex-shrink-0 mt-0.5" />
          <div>
            <h3 className="font-semibold text-sm text-gray-700 hover:underline">Fiado em Aberto</h3>
            <p className="text-xs text-gray-400">{clientes.length} clientes</p>
          </div>
        </Link>
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Testar na UI**

Com `npm run dev`:
- Sidebar desktop: hover expande e mostra "Fiado" entre Pedidos e Clientes com ícone de carteira
- Sidebar mobile: mesmo item aparece no drawer
- Dashboard: KPI "Fiado em Aberto" — clicar no header navega para `/fiado`
- Link `/fiado` na sidebar ativa highlight azul quando na página

- [ ] **Step 5: Commit e push**

```bash
git add src/components/layout/Sidebar.tsx src/components/dashboard/PainelFiado.tsx
git commit -m "$(cat <<'EOF'
feat: entrada Fiado na sidebar e link no PainelFiado do dashboard

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
git push
```
